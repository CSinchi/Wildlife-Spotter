# Wildlife-Spotter
IT 342 Project, docker images were setup to run the enviroment locally. Code is then deployed to corresponding EC2 instances in AWS.

Run locally using docker compose with command "docker-compose up --build" in directory of this project

Most of the codebase was generated by Google Gemini, with a few tweaks made manually.